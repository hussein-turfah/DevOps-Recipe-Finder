name: Deploy Streamlit App
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  Analyze:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5.2.0
        with:
          python-version: '3.11'

      - name: Install Packages
        run: |
          python3 -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Analyze Code using pylint
        run: |
          pylint app.py

  # Setting-Up:
  #   runs-on: ubuntu-latest
  #   needs: [Analyze]
        
  #   steps:
  #     - uses: actions/checkout@v4
  
  #     - name: Connect to EC2
  #       run: ssh -i ${{ secrets.SERVER_PEM }} ubuntu@${{ secrets.SERVER_IP }} "echo Connected to EC2"
        
